import React from 'react';

export default function HeaderFn() {
	const style = { fontWeight: 700, fontSize: '28px', color: 'rgb(40,80,180)' };
	return (
		<header style={style}>Moja nie taka już pierwsza strona w React :-)</header>
	);
}
